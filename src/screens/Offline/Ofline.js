import React, { Component } from "react";
import { Text, View } from "react-native";

export default class Offline extends Component {
  render() {
    return (
      <View>
        <Text> Sorry but u need connect to internet to use the App </Text>
      </View>
    );
  }
}
